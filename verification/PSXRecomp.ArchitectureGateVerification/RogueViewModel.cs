using PSXRecomp.Architecture;

namespace PSXRecompStudio.ViewModels;

// INTENTIONAL VERIFICATION ARTIFACT (Issue #105) - must never be merged to main.
// Namespace maps to the Application layer per docs/architecture-matrix.md.
[Application]
public sealed class RogueViewModel;
